## 1. Apply Font Stack

- [x] 1.1 Add a `body` rule to `src/routes/layout.css` setting `font-family` to the Rounded Sans stack: `ui-rounded, 'Hiragino Maru Gothic ProN', Quicksand, Comfortaa, Manjari, 'Arial Rounded MT', 'Arial Rounded MT Bold', Calibri, source-sans-pro, sans-serif`

## 2. Verify

- [x] 2.1 Run `bun run dev`, open the app, and confirm in browser DevTools that `body` has the correct `font-family` value
- [x] 2.2 Confirm no font file network requests appear in the DevTools Network tab
- [x] 2.3 Run `bun run check` and confirm 0 errors and 0 warnings
